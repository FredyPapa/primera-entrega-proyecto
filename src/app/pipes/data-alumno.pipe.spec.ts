import { DataAlumnoPipe } from './data-alumno.pipe';

describe('DataAlumnoPipe', () => {
  it('create an instance', () => {
    const pipe = new DataAlumnoPipe();
    expect(pipe).toBeTruthy();
  });
});
