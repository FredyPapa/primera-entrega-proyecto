import { DescripcionSexoPipe } from './descripcion-sexo.pipe';

describe('DescripcionSexoPipe', () => {
  it('create an instance', () => {
    const pipe = new DescripcionSexoPipe();
    expect(pipe).toBeTruthy();
  });
});
