import { EstiloSexoDirective } from './estilo-sexo.directive';

describe('EstiloSexoDirective', () => {
  it('should create an instance', () => {
    const directive = new EstiloSexoDirective();
    expect(directive).toBeTruthy();
  });
});
